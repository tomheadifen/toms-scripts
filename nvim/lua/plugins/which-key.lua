-- Shows a popup listing available keybindings when you press a prefix key (e.g. \) and pause.
-- Acts as a live cheatsheet built from your actual config.
return {
    'folke/which-key.nvim',
    event = 'VeryLazy',
    opts = {}
}
